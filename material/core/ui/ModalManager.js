/**
 * ModalManager.js
 * 
 * Handles modal dialogs.
 */

export class ModalManager {
  constructor(app) {
    this.app = app;
  }

  /**
   * Bind modal events
   */
  bind() {
    // No modals currently active
  }
}
